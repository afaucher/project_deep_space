# Godot-specific ignores
.godot/
!/.godot/extension_list.cfg
!/.godot/global_script_class_cache.cfg
!/.godot/uid_cache.bin

# Build output
build/
export_presets.cfg
*.tmp
*.bak
*.sav
*.log
test_logs/

# Test-run artifact (test_dump_contacts writes this to the repo root; it was
# removed from tracking in the scratchpad cleanup and keeps regenerating)
contacts_dump.txt

# Per-developer control remaps (InputBindings autoload writes this)
input_bindings.cfg

# Windows specific
desktop.ini
Thumbs.db
*.exe
*.zip

# Python (generate_audio.py helper)
__pycache__/
*.pyc
version.txt
